{
  "name": "akayroom",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "worker:build": "npm run build --prefix worker",
    "twin:worker": "npm run build --prefix worker && node worker/lib/index.js",
    "firebase:login": "firebase login",
    "firebase:use": "firebase use --add",
    "firebase:rules": "firebase deploy --only database"
  },
  "dependencies": {
    "firebase": "^12.1.0",
    "next": "^15.5.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "typescript": "^5.8.0"
  }
}
